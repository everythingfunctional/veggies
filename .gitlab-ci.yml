image: registry.gitlab.com/everythingfunctional/dockerfpm

variables:
    GIT_SUBMODULE_STRATEGY: normal

test:
  script:
    - fpm test
    - fpm test --runer "valgrind --leak-check=full --error-exitcode=1"
